<h1>Authors#new</h1>

<%= simple_form_for(@author) do |f| %>
	<%= f.input :name %>
	<%= f.input :email %>
	<%= f.input :password%>
	<%= f.input :password_confirmation%>
	<%= f.submit "Create"%>
<% end %>
